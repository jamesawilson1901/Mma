{
  "name": "anchor-pwa",
  "version": "1.0.0",
  "private": true,
  "description": "Anchor — an evidence-based behaviour-change PWA",
  "type": "module",
  "scripts": {
    "vapid": "web-push generate-vapid-keys",
    "icons": "node scripts/generate-icons.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.10",
    "web-push": "^3.6.7"
  }
}
