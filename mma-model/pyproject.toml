[project]
name = "mma-model"
version = "0.1.0"
description = "Multi-promotion MMA fight outcome prediction model (Glicko-2 + GBM)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
